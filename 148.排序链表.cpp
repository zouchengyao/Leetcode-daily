/*
 * @lc app=leetcode.cn id=148 lang=cpp
 *
 * [148] 排序链表
 */

// @lc code=start
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode(int x) : val(x), next(NULL) {}
 * };
 */
class Solution {
public:
    ListNode* sortList(ListNode* head) {
                // priority_queue<int, vector<int>, greater<int>  > p;
                // auto curr = head;
                // while(curr != nullptr)
                // {
                //     p.push(curr->val);
                //     curr = curr -> next;
                // }
                // auto sentiel = new ListNode(0);
                // curr = sentiel;
                // while( !p.empty() )
                // {
                //     auto newNode = new ListNode(p.top());
                //     curr -> next = newNode;
                //     curr = newNode;
                //     p.pop();
                // }
                // return sentiel -> next;

        // sort the list recursively 

        


    }
};
// @lc code=end

